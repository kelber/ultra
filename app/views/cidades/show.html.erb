<h1><%= image_tag("logoultra250CinzaEscuro.png", size: "100x100", alt: "Ultravisao") %> <%= @cidade.name %></h1>
<%= link_to 'Voltar', estados_path, class: "btn btn-default" %>

<hr>
	<% @cidade.lojas.each do |loja| %>
	<h3><%= link_to loja.name, loja %></h3>

	<% end %>